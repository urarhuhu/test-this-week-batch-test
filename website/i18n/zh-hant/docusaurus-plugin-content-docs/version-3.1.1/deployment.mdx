---
description: Deploy your Docusaurus app for production on a range of static site hosting services.
---

# Deployment

To build the static files of your website for production, run:

```bash npm2yarn
npm run build
```

Once it finishes, the static files will be generated within the `build` directory.

:::note

The only responsibility of Docusaurus is to build your site and emit static files in `build`.

It is now up to you to choose how to host those static files.

:::

You can deploy your site to static site hosting services such as [Vercel](https://vercel.com/), [GitHub Pages](https://pages.github.com/), [Netlify](https://www.netlify.com/), [Render](https://render.com/docs/static-sites), and [Surge](https://surge.sh/help/getting-started-with-surge).

A Docusaurus site is statically rendered, and it can generally work without JavaScript!

## Configuration {#configuration}

The following parameters are required in `docusaurus.config.js` to optimize routing and serve files from the correct location:

| Name | Description |
| --- | --- |
| `url` | URL for your site. For a site deployed at `https://my-org.com/my-project/`, `url` is `https://my-org.com/`. |
| `baseUrl` | Base URL for your project, with a trailing slash. For a site deployed at `https://my-org.com/my-project/`, `baseUrl` is `/my-project/`. |

## Testing your Build Locally {#testing-build-locally}

It is important to test your build locally before deploying it for production. Docusaurus provides a [`docusaurus serve`](cli.mdx#docusaurus-serve-sitedir) command for that:

```bash npm2yarn
npm run serve
```

By default, this will load your site at [`http://localhost:3000/`](http://localhost:3000/).

## Trailing slash configuration {#trailing-slashes}

Docusaurus has a [`trailingSlash` config](./api/docusaurus.config.js.mdx#trailingSlash) to allow customizing URLs/links and emitted filename patterns.

The default value generally works fine. Unfortunately, each static hosting provider has a **different behavior**, and deploying the exact same site to various hosts can lead to distinct results. Depending on your host, it can be useful to change this config.

:::tip

Use [slorber/trailing-slash-guide](https://github.com/slorber/trailing-slash-guide) to understand better the behavior of your host and configure `trailingSlash` appropriately.

:::

## Using environment variables {#using-environment-variables}

Putting potentially sensitive information in the environment is common practice. However, in a typical Docusaurus website, the `docusaurus.config.js` file is the only interface to the Node.js environment (see [our architecture overview](advanced/architecture.mdx)), while everything else (MDX pages, React components, etc.) are client side and do not have direct access to the `process` global variable. In this case, you can consider using [`customFields`](api/docusaurus.config.js.mdx#customFields) to pass environment variables to the client side.

```js title="docusaurus.config.js"
// If you are using dotenv (https://www.npmjs.com/package/dotenv)
import 'dotenv/config';

export default {
  title: '...',
  url: process.env.URL, // You can use environment variables to control site specifics as well
  // highlight-start
  customFields: {
    // Put your custom environment here
    teamEmail: process.env.EMAIL,
  },
  // highlight-end
};
```

```jsx title="home.jsx"
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';

export default function Home() {
  const {
    siteConfig: {customFields},
  } = useDocusaurusContext();
  return <div>Contact us through {customFields.teamEmail}!</div>;
}
```

## Choosing a hosting provider {#choosing-a-hosting-provider}

There are a few common hosting options:

- [Self hosting](#self-hosting) with an HTTP server like Apache2 or Nginx.
- Jamstack providers (e.g. [Netlify](#deploying-to-netlify) and [Vercel](#deploying-to-vercel)). We will use them as references, but the same reasoning can apply to other providers.
- [GitHub Pages](#deploying-to-github-pages) (by definition, it is also Jamstack, but we compare it separately).

If you are unsure of which one to choose, ask the following questions:

<details>

<summary>
  How many resources (money, person-hours, etc.) am I willing to invest in this?
</summary>

- 🔴 Self-hosting requires experience in networking as well as Linux and web server administration. It's the most difficult option, and would require the most time to manage successfully. Expense-wise, cloud services are almost never free, and purchasing/deploying an onsite server can be even more costly.
- 🟢 Jamstack providers can help you set up a working website in almost no time and offers features like server-side redirects that are easily configurable. Many providers offer generous build-time quotas even for free plans that you would almost never exceed. However, free plans have limits, and you would need to pay once you hit those limits. Check the pricing page of your provider for details.
- 🟡 The GitHub Pages deployment workflow can be tedious to set up. (Evidence: see the length of [Deploying to GitHub Pages](#deploying-to-github-pages)!) However, this service (including build and deployment) is always free for public repositories, and we have detailed instructions to help you make it work.

</details>

<details>

<summary>How much server-side customization do I need?</summary>

- 🟢 With self-hosting, you have access to the entire server's configuration. You can configure the virtual host to serve different content based on the request URL, you can do complicated server-side redirects, you can implement authentication, and so on. If you need a lot of server-side features, self-host your website.
- 🟡 Jamstack usually offers some server-side configuration (e.g. URL formatting (trailing slashes), server-side redirects, etc.).
- 🔴 GitHub Pages doesn't expose server-side configuration besides enforcing HTTPS and setting CNAME records.

</details>

<details>

<summary>Do I need collaboration-friendly deployment workflows?</summary>

- 🟡 Self-hosted services can leverage continuous deployment functionality like Netlify, but more heavy-lifting is involved. Usually, you would designate a specific person to manage the deployment, and the workflow wouldn't be very git-based as opposed to the other two options.
- 🟢 Netlify and Vercel have deploy previews for every pull request, which is useful for a team to review work before merging to production. You can also manage a team with different member access to the deployment.
- 🟡 GitHub Pages cannot do deploy previews in a non-convoluted way. One repo can only be associated with one site deployment. On the other hand, you can control who has write access to the site's deployment.

</details>

There isn't a silver bullet. You need to weigh your needs and resources before making a choice.

## 自行託管 {#self-hosting}

Docusaurus 可使用 [`docusaurus serve`](cli.mdx#docusaurus-serve-sitedir) 指令進行自行託管。透過 `--port` 變更連接埠，或使用 `--host` 變更主機位置。

```bash npm2yarn
npm run serve -- --build --port 80 --host 0.0.0.0
```

:::warning

相較於靜態網站託管服務/CDN，此非最佳選擇。

:::

:::warning

後續章節將介紹常見託管服務供應商及其配置方式，以最有效率地部署 Docusaurus 網站。Docusaurus 與這些服務無隸屬關係，此處資訊僅供參考。部分內容由第三方提供，最新API變動可能未即時更新。若發現過時內容，歡迎提交PR修正。

由於我們僅能盡力維護這些內容，我們已停止接受新增託管選項的PR。但您可將教學文章發表於其他平台（如個人部落格或供應商官網），並請我們加入文章連結。

:::

## 部署至 Netlify {#deploying-to-netlify}

要將 Docusaurus 網站部署至 [Netlify](https://www.netlify.com/)，請先確認以下設定已正確配置：

```js title="docusaurus.config.js"
export default {
  // highlight-start
  url: 'https://docusaurus-2.netlify.app', // Url to your site with no trailing slash
  baseUrl: '/', // Base directory of your site relative to your repo
  // highlight-end
  // ...
};
```

接著，[於 Netlify 建立您的網站](https://app.netlify.com/start)。

設定網站時，請依下列方式指定建置指令與目錄：

- 建置指令：`npm run build`
- 發佈目錄：`build`

若未事先設定這些建置選項，仍可於網站建立後，前往「Site settings」→「Build & deploy」進行配置。

完成上述設定後，您的網站將自動部署，並在程式碼合併至部署分支（預設為 `main`）時重新部署。

:::warning

部分 Docusaurus 網站將 `docs` 資料夾置於 `website` 外（多為舊版 Docusaurus v1 的結構）：

```bash
repo           # git 根目錄
├── docs       # Markdown 檔案
└── website    # Docusaurus 根目錄
```

若選擇將 `website` 資料夾設為 Netlify 的基礎目錄，當更新 `docs` 資料夾時 Netlify 不會觸發建置，此時需配置[自訂 `ignore` 指令](https://docs.netlify.com/configure-builds/common-configurations/ignore-builds/)：

```toml title="website/netlify.toml"
[build]
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF . ../docs/"
```

:::

:::warning

預設情況下，Netlify 會為 Docusaurus 網址添加尾部斜線。

建議停用 Netlify 的「Post Processing > Asset Optimization > Pretty Urls」設定，以避免網址被轉為小寫、產生不必要的重新導向及404錯誤。

**請特別注意**：全局選項「Disable asset optimization」實際無法停用「Pretty URLs」設定。請務必獨立取消勾選此選項。

若需保留 Netlify 的「Pretty Urls」設定，請相應調整 Docusaurus 的 `trailingSlash` 配置。

詳見 [slorber/trailing-slash-guide](https://github.com/slorber/trailing-slash-guide)。

:::

## 部署至 Vercel {#deploying-to-vercel}

將 Docusaurus 專案部署至 [Vercel](https://vercel.com/)，可獲得[效能與易用性等多重優勢](https://vercel.com/)。

要透過 [Vercel 的 Git 整合功能](https://vercel.com/docs/concepts/git)部署專案，請先確保專案已推送至 Git 儲存庫。

透過 [導入流程](https://vercel.com/import/git) 將專案導入 Vercel。在導入過程中，您會發現所有相關選項都已預先配置好；不過，您可以選擇更改其中任何 [選項](https://vercel.com/docs/build-step#build-&-development-settings)。

專案導入後，所有後續推送至分支的動作都會產生 [預覽部署](https://vercel.com/docs/platform/deployments#preview)，而對 [生產分支](https://vercel.com/docs/git-integrations#production-branch)（通常是 "main" 或 "master"）所做的任何更改都會觸發 [生產部署](https://vercel.com/docs/platform/deployments#production)。

## 部署至 GitHub Pages {#deploying-to-github-pages}

Docusaurus 提供了一種簡單的方式來發布到 [GitHub Pages](https://pages.github.com/)，這項服務免費提供給每個 GitHub 儲存庫使用。

### 概述 {#github-pages-overview}

通常，發布流程會涉及兩個儲存庫（至少兩個分支）：包含原始檔的分支，以及包含要透過 GitHub Pages 服務的建置輸出的分支。在以下教學中，它們將分別被稱為「原始」和「部署」分支。

每個 GitHub 儲存庫都與一項 GitHub Pages 服務相關聯。如果部署儲存庫名為 `my-org/my-project`（其中 `my-org` 是組織名稱或用戶名），則部署的網站將出現在 `https://my-org.github.io/my-project/`。如果部署儲存庫名為 `my-org/my-org.github.io`（即 _組織 GitHub Pages 儲存庫_），則網站將出現在 `https://my-org.github.io/`。

:::info

如果您想為 GitHub Pages 使用自訂網域，請在 `static` 目錄中建立一個 `CNAME` 檔案。`static` 目錄中的任何內容都會被複製到 `build` 目錄的根目錄以進行部署。使用自訂網域時，您應該可以將 `baseUrl: '/projectName/'` 改回 `baseUrl: '/'`，並將 `url` 設定為您的自訂網域。

您可以參考 GitHub Pages 的文件 [用戶、組織和專案頁面](https://help.github.com/en/articles/user-organization-and-project-pages) 以獲取更多詳細資訊。

:::

GitHub Pages 會從預設分支（通常是 `master` 或 `main`）或 `gh-pages` 分支，以及從根目錄或 `/docs` 資料夾中提取部署就緒的檔案（`docusaurus build` 的輸出）。您可以透過儲存庫中的 `Settings > Pages` 來配置這一點。這個分支將被稱為「部署分支」。

我們提供了一個 `docusaurus deploy` 命令，幫助您透過一個命令將網站從原始分支部署到部署分支：克隆、建置和提交。

### `docusaurus.config.js` 設定 {#docusaurusconfigjs-settings}

首先，修改您的 `docusaurus.config.js` 並添加以下參數：

| Name | Description |
| --- | --- |
| `organizationName` | The GitHub user or organization that owns the deployment repository. |
| `projectName` | The name of the deployment repository. |
| `deploymentBranch` | The name of the deployment branch. It defaults to `'gh-pages'` for non-organization GitHub Pages repos (`projectName` not ending in `.github.io`). Otherwise, it needs to be explicit as a config field or environment variable. |

這些字段也有其對應的環境變數，這些變數具有更高的優先級：`ORGANIZATION_NAME`、`PROJECT_NAME` 和 `DEPLOYMENT_BRANCH`。

:::warning

GitHub Pages 預設會為 Docusaurus URL 添加尾部斜線。建議設定 `trailingSlash` 配置（`true` 或 `false`，而非 `undefined`）。

:::

範例：

```js title="docusaurus.config.js"
export default {
  // ...
  url: 'https://endiliey.github.io', // Your website URL
  baseUrl: '/',
  // highlight-start
  projectName: 'endiliey.github.io',
  organizationName: 'endiliey',
  trailingSlash: false,
  // highlight-end
  // ...
};
```

:::warning

預設情況下，GitHub Pages 會透過 [Jekyll](https://jekyllrb.com/) 處理發布的檔案。由於 Jekyll 會丟棄任何以 `_` 開頭的檔案，建議您透過在 `static` 目錄中添加一個名為 `.nojekyll` 的空檔案來停用 Jekyll。

:::

### 環境設定 {#environment-settings}

| Name | Description |
| --- | --- |
| `USE_SSH` | Set to `true` to use SSH instead of the default HTTPS for the connection to the GitHub repo. If the source repo URL is an SSH URL (e.g. `git@github.com:facebook/docusaurus.git`), `USE_SSH` is inferred to be `true`. |
| `GIT_USER` | The username for a GitHub account that **has push access to the deployment repo**. For your own repositories, this will usually be your GitHub username. Required if not using SSH, and ignored otherwise. |
| `GIT_PASS` | Personal access token of the git user (specified by `GIT_USER`), to facilitate non-interactive deployment (e.g. continuous deployment) |
| `CURRENT_BRANCH` | The source branch. Usually, the branch will be `main` or `master`, but it could be any branch except for `gh-pages`. If nothing is set for this variable, then the current branch from which `docusaurus deploy` is invoked will be used. |
| `GIT_USER_NAME` | The `git config user.name` value to use when pushing to the deployment repo |
| `GIT_USER_EMAIL` | The `git config user.email` value to use when pushing to the deployment repo |

GitHub 企業版安裝應以與 github.com 相同的方式運作；您只需將組織的 GitHub Enterprise 主機設定為環境變數：

| Name          | Description                                     |
| ------------- | ----------------------------------------------- |
| `GITHUB_HOST` | The domain name of your GitHub enterprise site. |
| `GITHUB_PORT` | The port of your GitHub enterprise site.        |

### 部署 {#deploy}

最後，要將您的網站部署到 GitHub Pages，請執行：

```mdx-code-block
<Tabs>
<TabItem value="bash" label="Bash">
```

```bash
GIT_USER=<GITHUB_USERNAME> yarn deploy
```

```mdx-code-block
</TabItem>
<TabItem value="windows" label="Windows">
```

```batch
cmd /C "set "GIT_USER=<GITHUB_USERNAME>" && yarn deploy"
```

```mdx-code-block
</TabItem>
<TabItem value="powershell" label="PowerShell">
```

```powershell
cmd /C 'set "GIT_USER=<GITHUB_USERNAME>" && yarn deploy'
```

```mdx-code-block
</TabItem>
</Tabs>
```

:::warning

自 2021 年 8 月起，GitHub 要求所有命令行登入使用**個人存取權杖 (PAT)** 而非密碼。當 GitHub 提示輸入密碼時，請改為輸入 PAT。詳情請參閱 [GitHub 文件](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token)。

或者，您也可以使用 SSH (`USE_SSH=true`) 登入。

:::

### 使用 GitHub Actions 觸發部署 {#triggering-deployment-with-github-actions}

[GitHub Actions](https://help.github.com/en/actions) 讓您可以直接在儲存庫中自動化、自訂並執行軟體開發工作流程。

以下工作流程範例假設您的網站原始碼位於儲存庫的 `main` 分支（_原始碼分支_ 為 `main`），且您的 [發布來源](https://help.github.com/en/github/working-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site) 設定為 `gh-pages` 分支（_部署分支_ 為 `gh-pages`）。

我們的目標是：

1. 當有新的拉取請求 (PR) 提交至 `main` 分支時，執行一個確保網站能成功建置的動作，但不會實際部署。此工作將稱為 `test-deploy`。
2. 當拉取請求合併至 `main` 分支或有人直接推送至 `main` 分支時，將建置並部署至 `gh-pages` 分支。之後，新的建置輸出將在 GitHub Pages 網站上提供服務。此工作將稱為 `deploy`。

以下是兩種使用 GitHub Actions 部署文件的方法。根據您的部署分支 (`gh-pages`) 的位置，選擇下方相關的標籤：

- 原始碼儲存庫和部署儲存庫為**同一個**儲存庫。
- 部署儲存庫是一個**遠端**儲存庫，與原始碼儲存庫不同。

```mdx-code-block
<Tabs>
<TabItem value="same" label="Same">
```

雖然您可以將兩個工作定義在同一個工作流程檔案中，但原始的 `deploy` 工作流程在 PR 檢查套件狀態中會始終顯示為已跳過，這並不能反映實際狀態，也無法為審查過程提供價值。因此，我們建議將它們作為獨立的工作流程來管理。

我們將使用一個受歡迎的第三方部署動作：[peaceiris/actions-gh-pages](https://github.com/peaceiris/actions-gh-pages#%EF%B8%8F-docusaurus)。

<details>
<summary>GitHub action files</summary>

Add these two workflow files:

:::warning[Tweak the parameters for your setup]

These files assume you are using Yarn. If you use npm, change `cache: yarn`, `yarn install --frozen-lockfile`, `yarn build` to `cache: npm`, `npm ci`, `npm run build` accordingly.

If your Docusaurus project is not at the root of your repo, you may need to configure a [default working directory](https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#example-set-the-default-shell-and-working-directory), and adjust the paths accordingly.

:::

```yml title=".github/workflows/deploy.yml"
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    # Review gh actions docs if you want to further define triggers, paths, etc
    # https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#on

permissions:
  contents: write

jobs:
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Build website
        run: yarn build

      # Popular action to deploy to GitHub Pages:
      # Docs: https://github.com/peaceiris/actions-gh-pages#%EF%B8%8F-docusaurus
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Build output to publish to the `gh-pages` branch:
          publish_dir: ./build
          # The following lines assign commit authorship to the official
          # GH-Actions bot for deploys to `gh-pages` branch:
          # https://github.com/actions/checkout/issues/13#issuecomment-724415212
          # The GH actions bot is used by default if you didn't specify the two fields.
          # You can swap them out with your own user credentials.
          user_name: github-actions[bot]
          user_email: 41898282+github-actions[bot]@users.noreply.github.com
```

```yml title=".github/workflows/test-deploy.yml"
name: Test deployment

on:
  pull_request:
    branches:
      - main
    # Review gh actions docs if you want to further define triggers, paths, etc
    # https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#on

jobs:
  test-deploy:
    name: Test deployment
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Test build website
        run: yarn build
```

</details>

```mdx-code-block
</TabItem>
<TabItem value="remote" label="Remote">
```

跨儲存庫發布的設定較為複雜，因為您需要有權限推送至另一個儲存庫。我們將使用 SSH 進行身份驗證。

1. 產生一個新的 [SSH 金鑰](https://help.github.com/en/github/authenticating-to-github/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent)。由於此 SSH 金鑰將用於 CI 環境，請確保不要輸入任何密碼。
2. 預設情況下，您的公開金鑰應建立於 `~/.ssh/id_rsa.pub`；若非如此，請使用您在前一步驟中提供的名稱，將金鑰新增至 [GitHub 部署金鑰](https://developer.github.com/v3/guides/managing-deploy-keys/)。
3. 使用 `pbcopy < ~/.ssh/id_rsa.pub` 將金鑰複製到剪貼簿，並將其貼上作為部署儲存庫中的 [部署金鑰](https://developer.github.com/v3/guides/managing-deploy-keys/#deploy-keys)。如果命令列無法運作，請複製檔案內容。在儲存部署金鑰之前，勾選 `允許寫入存取` 的選項。
4. 您需要將您的私密金鑰設定為 [GitHub 秘密](https://help.github.com/en/actions/configuring-and-managing-workflows/creating-and-storing-encrypted-secrets)，以允許 Docusaurus 為您執行部署。
5. 使用 `pbcopy < ~/.ssh/id_rsa` 複製您的私密金鑰，並在您的原始碼儲存庫上貼上一個名為 `GH_PAGES_DEPLOY` 的 GitHub 秘密。如果命令列無法運作，請複製檔案內容。儲存您的秘密。
6. 在 `.github/workflows/` 目錄中建立您的 [文件工作流程](https://docs.github.com/en/actions/use-cases-and-examples/creating-an-example-workflow)。在此範例中，它是 `deploy.yml` 檔案。

此時，您應該具備：

- 原始碼儲存庫已設定 GitHub 工作流程，並將私密 SSH 金鑰設為 GitHub 秘密，以及
- 您的部署儲存庫已在 GitHub 部署金鑰中設定公開 SSH 金鑰。

<details>

<summary>GitHub action file</summary>

:::warning

Please make sure that you replace `actions@github.com` with your GitHub email and `gh-actions` with your name.

This file assumes you are using Yarn. If you use npm, change `cache: yarn`, `yarn install --frozen-lockfile`, `yarn build` to `cache: npm`, `npm ci`, `npm run build` accordingly.

:::

```yml title=".github/workflows/deploy.yml"
name: Deploy to GitHub Pages

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  test-deploy:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn
      - name: Install dependencies
        run: yarn install --frozen-lockfile
      - name: Test build website
        run: yarn build
  deploy:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn
      - uses: webfactory/ssh-agent@v0.5.0
        with:
          ssh-private-key: ${{ secrets.GH_PAGES_DEPLOY }}
      - name: Deploy to GitHub Pages
        env:
          USE_SSH: true
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "gh-actions"
          yarn install --frozen-lockfile
          yarn deploy
```

</details>

```mdx-code-block
</TabItem>
</Tabs>
```

<details>

<summary>Site not deployed properly?</summary>

After pushing to main, if you don't see your site published at the desired location (for example, it says "There isn't a GitHub Pages site here", or it's showing your repo's README.md file), try the following:

- Wait about three minutes and refresh. It may take a few minutes for GitHub pages to pick up the new files.
- Check your repo's landing page for a little green tick next to the last commit's title, indicating the CI has passed. If you see a cross, it means the build or deployment failed, and you should check the log for more debugging information.
- Click on the tick and make sure you see a "Deploy to GitHub Pages" workflow. Names like "pages build and deployment / deploy" are GitHub's default workflows, indicating your custom deployment workflow failed to be triggered at all. Make sure the YAML files are placed under the `.github/workflows` folder, and that the trigger condition is set correctly (e.g., if your default branch is "master" instead of "main", you need to change the `on.push` property).
- Under your repo's Settings > Pages, make sure the "Source" (which is the source for the _deployment_ files, not "source" as in our terminology) is set to "gh-pages" + "/ (root)", since we are using `gh-pages` as the deployment branch.

If you are using a custom domain:

- Verify that you have the correct DNS records set up if you're using a custom domain. See [GitHub pages documentation on configuring custom domains](https://docs.github.com/en/pages/configuring-a-custom-domain-for-your-github-pages-site/about-custom-domains-and-github-pages). Also, please be aware that it may take up to 24 hours for DNS changes to propagate through the internet.

</details>

### 使用 Travis CI 觸發部署 {#triggering-deployment-with-travis-ci}

持續整合 (CI) 服務通常用於在每次新提交檢入原始碼控制時執行例行任務。這些任務可以是執行單元測試和整合測試、自動化建置、將套件發佈到 npm，以及將變更部署到您的網站。要自動化網站的部署，您只需在網站更新時呼叫 `yarn deploy` 腳本。以下部分介紹如何使用 [Travis CI](https://travis-ci.com/)（一個流行的持續整合服務提供商）來實現這一點。

1. 前往 https://github.com/settings/tokens 並產生一個新的 [個人存取權杖](https://help.github.com/articles/creating-a-personal-access-token-for-the-command-line/)。在建立權杖時，授予其 `repo` 範圍，以便其擁有所需的權限。
2. 使用您的 GitHub 帳戶，[新增 Travis CI 應用程式](https://github.com/marketplace/travis-ci) 到您要啟用的儲存庫。
3. 開啟您的 Travis CI 儀表板。網址類似於 `https://travis-ci.com/USERNAME/REPO`，並導航至您的儲存庫的 `更多選項 > 設定 > 環境變數` 部分。
4. 建立一個名為 `GH_TOKEN` 的新環境變數，其值為您新產生的權杖，然後是 `GH_EMAIL`（您的電子郵件地址）和 `GH_NAME`（您的 GitHub 使用者名稱）。
5. 在您的儲存庫根目錄中建立一個 `.travis.yml`，內容如下：

```yml title=".travis.yml"
language: node_js
node_js:
  - 18
branches:
  only:
    - main
cache:
  yarn: true
script:
  - git config --global user.name "${GH_NAME}"
  - git config --global user.email "${GH_EMAIL}"
  - echo "machine github.com login ${GH_NAME} password ${GH_TOKEN}" > ~/.netrc
  - yarn install
  - GIT_USER="${GH_NAME}" yarn deploy
```

現在，每當有新提交進入 `main` 分支時，Travis CI 將執行您的測試套件，如果一切通過，您的網站將透過 `yarn deploy` 腳本進行部署。

### 使用 Buddy 觸發部署 {#triggering-deployment-with-buddy}

[Buddy](https://buddy.works/) 是一個易於使用的 CI/CD 工具，允許您自動將入口網站部署到不同的環境，包括 GitHub Pages。

按照以下步驟建立一個管道，該管道會在您將變更推送到所選專案分支時自動部署網站的新版本：

1. 前往 https://github.com/settings/tokens 生成一個新的[個人存取權杖](https://help.github.com/articles/creating-a-personal-access-token-for-the-command-line/)。創建權杖時，請授予其 `repo` 範圍以確保其擁有所需權限。
2. 登入您的 Buddy 帳戶並創建一個新專案。
3. 選擇 GitHub 作為您的 git 託管服務提供商，並選擇包含您網站代碼的存儲庫。
4. 使用左側導航面板切換至 `Pipelines` 視圖。
5. 創建一個新管道。定義其名稱，將觸發模式設置為 `On push`，並選擇觸發管道執行的分支。
6. 添加一個 `Node.js` 操作。
7. 在操作的終端中添加以下命令：

```bash
GIT_USER=<GH_PERSONAL_ACCESS_TOKEN>
git config --global user.email "<YOUR_GH_EMAIL>"
git config --global user.name "<YOUR_GH_USERNAME>"
yarn deploy
```

創建這個簡單管道後，每次推送至您選擇的分支的新提交都會使用 `yarn deploy` 將您的網站部署至 GitHub Pages。閱讀[本指南](https://buddy.works/guides/react-docusaurus)以了解更多關於為 Docusaurus 設置 CI/CD 管道的資訊。

### 使用 Azure Pipelines {#using-azure-pipelines}

1. 如果您尚未註冊，請先於 [Azure Pipelines](https://azure.microsoft.com/en-us/services/devops/pipelines/) 註冊。
2. 創建一個組織。在組織內創建一個專案並從 GitHub 連接您的存儲庫。
3. 前往 https://github.com/settings/tokens 生成一個新的[個人存取權杖](https://help.github.com/articles/creating-a-personal-access-token-for-the-command-line/)，並授予其 `repo` 範圍。
4. 在專案頁面（類似 `https://dev.azure.com/ORG_NAME/REPO_NAME/_build`）中，創建一個新管道並填入以下文本。同時，點擊編輯並添加一個名為 `GH_TOKEN` 的環境變量，其值為您新生成的權杖，然後添加 `GH_EMAIL`（您的電子郵件地址）和 `GH_NAME`（您的 GitHub 用戶名）。請確保將它們標記為秘密。或者，您也可以在存儲庫根目錄添加一個名為 `azure-pipelines.yml` 的文件。

```yml title="azure-pipelines.yml"
trigger:
  - main

pool:
  vmImage: ubuntu-latest

steps:
  - checkout: self
    persistCredentials: true

  - task: NodeTool@0
    inputs:
      versionSpec: '18'
    displayName: Install Node.js

  - script: |
      git config --global user.name "${GH_NAME}"
      git config --global user.email "${GH_EMAIL}"
      git checkout -b main
      echo "machine github.com login ${GH_NAME} password ${GH_TOKEN}" > ~/.netrc
      yarn install
      GIT_USER="${GH_NAME}" yarn deploy
    env:
      GH_NAME: $(GH_NAME)
      GH_EMAIL: $(GH_EMAIL)
      GH_TOKEN: $(GH_TOKEN)
    displayName: Install and build
```

### 使用 Drone {#using-drone}

1. 創建一個新的 SSH 金鑰，作為您專案的[部署金鑰](https://docs.github.com/en/free-pro-team@latest/developers/overview/managing-deploy-keys#deploy-keys)。
2. 為您的私鑰和公鑰命名，使其具有特定性且不會覆蓋您的其他[SSH 金鑰](https://docs.github.com/en/free-pro-team@latest/github/authenticating-to-github/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent)。
3. 前往 `https://github.com/USERNAME/REPO/settings/keys` 並添加一個新的部署金鑰，貼上您剛剛生成的公鑰。
4. 打開您的 Drone.io 儀表板並登入。URL 類似 `https://cloud.drone.io/USERNAME/REPO`。
5. 點擊存儲庫，點擊激活存儲庫，並添加一個名為 `git_deploy_private_key` 的秘密，其值為您剛剛生成的私鑰。
6. 在存儲庫根目錄創建一個 `.drone.yml` 文件，並填入以下文本。

```yml title=".drone.yml"
kind: pipeline
type: docker
trigger:
  event:
    - tag
- name: Website
  image: node
  commands:
    - mkdir -p $HOME/.ssh
    - ssh-keyscan -t rsa github.com >> $HOME/.ssh/known_hosts
    - echo "$GITHUB_PRIVATE_KEY" > "$HOME/.ssh/id_rsa"
    - chmod 0600 $HOME/.ssh/id_rsa
    - cd website
    - yarn install
    - yarn deploy
  environment:
    USE_SSH: true
    GITHUB_PRIVATE_KEY:
      from_secret: git_deploy_private_key
```

現在，每當您推送一個新標籤至 GitHub 時，此觸發器將啟動 drone CI 作業以發布您的網站。

## 部署至 Flightcontrol {#deploying-to-flightcontrol}

[Flightcontrol](https://www.flightcontrol.dev/?ref=docusaurus) 是一項服務，可自動從您的 Git 存儲庫構建並將您的 Web 應用部署至 AWS Fargate。它讓您可以完全檢查並進行基礎架構變更，而不受傳統 PaaS 的限制。

按照 [Flightcontrol 的 Docusaurus 逐步指南](https://www.flightcontrol.dev/docs/reference/examples/docusaurus/?ref=docusaurus) 開始使用。

## 部署至 Koyeb {#deploying-to-koyeb}

[Koyeb](https://www.koyeb.com) 是一個對開發者友善的無伺服器平台，可全球部署應用程式。該平台讓您能無縫運行 Docker 容器、網路應用程式和 API，並提供基於 Git 的部署、原生自動擴展、全球邊緣網路，以及內建的服務網格和服務發現功能。查看 [Koyeb 的 Docusaurus 部署指南](https://www.koyeb.com/tutorials/deploy-docusaurus-on-koyeb) 以開始使用。

## 部署至 Render {#deploying-to-render}

[Render](https://render.com) 提供 [免費的靜態網站託管服務](https://render.com/docs/static-sites)，包含全託管的 SSL、自訂網域、全球 CDN，以及從您的 Git 倉庫自動持續部署。只需幾分鐘即可開始，請遵循 [Render 的 Docusaurus 部署指南](https://render.com/docs/deploy-docusaurus)。

## 部署至 Qovery {#deploying-to-qovery}

[Qovery](https://www.qovery.com) 是一個全託管的雲端平台，運行於您的 AWS、Digital Ocean 和 Scaleway 帳戶上，您可以在同一個地方託管靜態網站、後端 API、資料庫、定時任務以及所有其他應用程式。

1. 建立 Qovery 帳戶。如果您還沒有帳戶，請訪問 [Qovery 儀表板](https://console.qovery.com) 建立一個帳戶。
2. 建立專案。
   - 點擊 **建立專案** 並為您的專案命名。
   - 點擊 **下一步**。
3. 建立新環境。
   - 點擊 **建立環境** 並命名（例如 staging、production）。
4. 新增應用程式。
   - 點擊 **建立應用程式**，命名並選擇您的 GitHub 或 GitLab 倉庫，其中包含您的 Docusaurus 應用程式。
   - 定義主分支名稱和應用程式的根路徑。
   - 點擊 **建立**。應用程式建立後：
   - 導航至您的應用程式 **設定**
   - 選擇 **連接埠**
   - 新增您的 Docusaurus 應用程式使用的連接埠
5. 部署
   - 現在您只需導航至您的應用程式並點擊 **部署**。

![部署應用程式](https://hub.qovery.com/img/heroku/heroku-1.png)

就是這樣。查看狀態並等待應用程式部署完成。要在瀏覽器中開啟應用程式，請在應用程式概覽中點擊 **操作** 和 **開啟**。

## 部署至 Hostman {#deploying-to-hostman}

[Hostman](https://hostman.com/) 允許您免費託管靜態網站。Hostman 會自動化所有流程，您只需連接您的倉庫並遵循這些簡單步驟：

1. 建立服務。

   - 若要部署 Docusaurus 靜態網站，請在 [儀表板](https://dashboard.hostman.com/) 左上角點擊 **建立** 並選擇 **前端應用或靜態網站**。

2. 選擇要部署的專案。

   - 若您使用 GitHub、GitLab 或 Bitbucket 帳號登入 Hostman，您將看到包含私有專案在內的儲存庫。

   - 選擇您想部署的專案。該專案必須包含專案檔案目錄（例如 `website`）。

   - 若要存取其他儲存庫，請點擊 **連接其他儲存庫**。

   - 若您未使用 Git 帳號憑證登入，此時可登入必要帳號後選擇專案。

3. 設定建置選項。

   - 接著會出現 **網站自訂** 視窗。從框架清單中選擇 **靜態網站** 選項。

   - **應用程式目錄** 指向建置後包含專案檔案的目錄。若您在步驟 2 選擇了包含網站內容（或 `my_website`）目錄的儲存庫，可留空此欄位。

   - Docusaurus 的標準建置指令為：

     ```bash npm2yarn
     npm run build
     ```

   - 可視需求修改建置指令。可輸入多個以 `&&` 分隔的指令。

4. 部署。

   - 點擊 **部署** 開始建置流程。

   - 啟動後將進入部署日誌。若程式碼有問題，日誌中會顯示標明問題原因的警告或錯誤訊息。通常日誌包含所有除錯所需資料。

   - 部署完成後，您將收到電子郵件通知並看到日誌記錄。大功告成！您的專案已上線就緒。

## 部署至 Surge {#deploying-to-surge}

Surge 是 [靜態網頁託管平台](https://surge.sh/help/getting-started-with-surge)，可讓您在數秒內從命令列部署 Docusaurus 專案。部署專案至 Surge 簡單且免費（包含自訂網域與 SSL 憑證）。

透過以下步驟，使用 surge 在數秒內部署您的應用：

1. 首先，執行以下命令透過 npm 安裝 Surge：
   ```bash npm2yarn
   npm install -g surge
   ```
2. 在專案根目錄執行以下命令，為生產環境建置靜態檔案：
   ```bash npm2yarn
   npm run build
   ```
3. 接著，在專案根目錄執行此命令：
   ```bash
   surge build/
   ```

首次使用 Surge 時，系統會提示您從命令列建立帳號（僅需一次）。

確認要發佈的網站位於 `build` 目錄。系統會隨機產生 `*.surge.sh 子網域`（可編輯）。

### 使用自訂網域 {#using-your-domain}

若您擁有網域名稱，可使用以下命令部署網站：

```bash
surge build/ your-domain.com
```

您的網站現已免費部署於 `subdomain.surge.sh` 或 `your-domain.com`（依您選擇的方法而定）。

### 設定 CNAME 檔案 {#setting-up-cname-file}

透過以下命令將網域儲存於 CNAME 檔案，供未來部署使用：

```bash
echo subdomain.surge.sh > CNAME
```

未來可透過 `surge` 命令部署任何變更。

## 部署至 QuantCDN {#deploying-to-quantcdn}

1. 安裝 [Quant CLI](https://docs.quantcdn.io/docs/cli/get-started)
2. 透過 [註冊](https://dashboard.quantcdn.io/register) 建立 QuantCDN 帳號
3. 使用 `quant init` 初始化專案並填寫憑證：
   ```bash
   quant init
   ```
4. 部署您的網站。
   ```bash
   quant deploy
   ```

See [docs](https://docs.quantcdn.io/docs/cli/continuous-integration) and [blog](https://www.quantcdn.io/blog) for more examples and use cases for deploying to QuantCDN.

## Deploying to Layer0 {#deploying-to-layer0}

[Layer0](https://www.layer0.co) is an all-in-one platform to develop, deploy, preview, experiment on, monitor, and run your headless frontend. It is focused on large, dynamic websites and best-in-class performance through EdgeJS (a JavaScript-based Content Delivery Network), predictive prefetching, and performance monitoring. Layer0 offers a free tier. Get started in just a few minutes by following [Layer0's guide to deploying Docusaurus](https://docs.layer0.co/guides/docusaurus).

## Deploying to Cloudflare Pages {#deploying-to-cloudflare-pages}

[Cloudflare Pages](https://pages.cloudflare.com/) is a Jamstack platform for frontend developers to collaborate and deploy websites. Get started within a few minutes by following [this article](https://dev.to/apidev234/deploying-docusaurus-to-cloudflare-pages-565g).

## Deploying to Azure Static Web Apps {#deploying-to-azure-static-web-apps}

[Azure Static Web Apps](https://docs.microsoft.com/en-us/azure/static-web-apps/overview) is a service that automatically builds and deploys full-stack web apps to Azure directly from the code repository, simplifying the developer experience for CI/CD. Static Web Apps separates the web application's static assets from its dynamic (API) endpoints. Static assets are served from globally-distributed content servers, making it faster for clients to retrieve files using servers nearby. Dynamic APIs are scaled with serverless architectures using an event-driven functions-based approach that is more cost-effective and scales on demand. Get started in a few minutes by following [this step-by-step guide](https://dev.to/azure/11-share-content-with-docusaurus-azure-static-web-apps-30hc).

## Deploying to Kinsta {#deploying-to-kinsta}

[Kinsta Static Site Hosting](https://kinsta.com/static-site-hosting) lets you deploy up to 100 static sites for free, custom domains with SSL, 100 GB monthly bandwidth, and 260+ Cloudflare CDN locations.

Get started in just a few clicks by following our [Docusaurus on Kinsta](https://kinsta.com/docs/docusaurus-example/) article.