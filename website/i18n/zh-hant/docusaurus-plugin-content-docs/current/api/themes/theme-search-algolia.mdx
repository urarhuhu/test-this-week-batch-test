---
sidebar_position: 4
slug: /api/themes/@docusaurus/theme-search-algolia
---

# 📦 theme-search-algolia

此主題提供了一個可輕鬆與 Algolia DocSearch 整合的 `@theme/SearchBar` 組件。與 `@docusaurus/theme-classic` 結合使用時，能實現極簡的搜索整合功能。更多資訊請參閱[搜索功能](../../search.mdx)文件。

```bash npm2yarn
npm install --save @docusaurus/theme-search-algolia
```

本主題還新增了可通過 `/search` 路徑訪問的搜索頁面（作為可替換的 `SearchPage` 組件），並支援 OpenSearch 協議。您可通過 `themeConfig.algolia.searchPagePath` 修改默認路徑，設置為 `false` 則會禁用搜索頁面。

:::tip

若已安裝 `@docusaurus/preset-classic`，則無需額外安裝本主題作為依賴項。

:::